# B44 Tooling Traps

This file is generated from canonical `B44.Standards` guidance. Change the source in B44.Common rather than editing a consumer's generated copy.

## Formatting and analyzers

- A `dotnet format --verify-no-changes` validation run is different from an automated fixer run. For fixes, use `dotnet format whitespace <solution>` plus narrowly targeted `dotnet format style <solution> --severity info --diagnostics <known-fixable IDs>` invocations.
- Do not put `IDE0130` or `MA0051` in a fixer diagnostic list. Their current fix paths are unsupported; repair namespace/folder mismatches and overlong methods deliberately by hand.
- Analyzer severities belong only in B44.Standards' packaged globalconfig. A repository `.editorconfig` outranks global configs and can create an unresolvable policy conflict.
- On Windows with `core.autocrlf=true` and no `.gitattributes`, `dotnet format --verify-no-changes` reports a mass of `ENDOFLINE` errors that do not exist in the committed blobs or in CI. Working files are CRLF, committed content is LF, and the editorconfig wants LF. Do NOT "fix" these by rewriting line endings — that is a repository-wide diff that hides the real change. To check formatting honestly, verify against an LF checkout: `git -c core.autocrlf=false worktree add <tmp> HEAD`, apply the change there with `git -c core.autocrlf=false apply`, and run the format check inside that worktree. Both `git apply` and `git checkout` re-apply autocrlf unless told otherwise, so the flag is needed on each.
- **Never filter that output down to a list of error categories you expect.** The spurious-`ENDOFLINE` problem tempts you to grep for the categories you care about; do that and the check silently stops covering every other category. `IMPORTS` is the one that actually got through — a real using-ordering violation reported clean locally and failed CI. In an LF worktree the honest assertion is that the total is zero, so count every diagnostic and compare against zero:

  ```bash
  dotnet format --verify-no-changes 2>&1 | grep -cE "error "
  ```

  Filtering by category is only ever valid in a CRLF working tree, where it is a triage aid and not a verification — a "clean" result there proves nothing about the categories you filtered out.

## Build isolation

- Concurrent repositories can cross-talk through reused MSBuild nodes and the shared compiler server on this machine. Run build and test verification with `/nodeReuse:false -p:UseSharedCompilation=false -m:1` so another repository's diagnostics cannot contaminate the result.

## Baselines, text, and packages

- Before measuring a clean baseline, remove only the intended repository's resolved `bin` and `obj` directories. Build products survive checkout and reset and can fake the result.
- **Measure an analyzer bump against every consumer before publishing it, not after.**
  A clean build in the package repository proves nothing about the games: they
  enable stricter rules, so the repository that ships the analyzer is the
  weakest place to test it. Add the candidate version as a temporary direct
  `PackageReference` in each consumer, rebuild, and count diagnostics. Shipping
  Meziantou 3.0.137 blind produced 380 build errors across three games and a
  same-day revert; measuring 3.0.138 the same way first took minutes and showed
  zero. The rule that an enforcement-expanding change takes a minor still
  applies either way — the measurement tells you what the migration costs, not
  whether you can skip the boundary.
- **After changing an analyzer package version, verify with `-t:Rebuild`.** MSBuild does not re-run analyzers on a project whose own inputs have not changed, and bumping a package that only the *analyzer* set depends on frequently does not invalidate a test or app project. An ordinary incremental build then reports zero diagnostics while a clean checkout reports many. This is not the stale-`bin` trap above: the outputs are legitimately up to date, and the diagnostics simply never ran. Verified 2026-08-01 — a local incremental build reported a repository clean on a new analyzer version, and CI failed it on five sites in the same commit.
- Preserve each edited file's existing encoding, BOM, final newline, and line-ending style. Do not let a bulk text-mode rewrite silently normalize unrelated lines.
- Immediately after publishing a new package version, use `dotnet restore --no-cache` in its first consumer validation so a stale local HTTP index cannot masquerade as the new package being absent.
- **`--no-cache` on `restore` is not enough, because `dotnet build` runs its own implicit restore that does not inherit the flag.** A build immediately after a clean restore can still read the cached index and report the new version missing. Either pass `--no-restore` to the build, or clear the cache outright with `dotnet nuget locals http-cache --clear`. The failure is convincing in the wrong direction: NuGet reports `Found N version(s) in nuget.org [ Nearest version: <previous> ]`, which reads like the package never published. Verified 2026-08-03 — ten minutes were spent waiting for CDN propagation that had already happened, and deleting the global package folder made it worse by discarding the copy that would have worked.
